<div class="col-md-12">
  <h1><%= @poll.title.faderize %></h1>
  <p><%= @poll.description %></p>
  <h3>Don't see your movie? Add it!</h3>
  <form method="post" action="/add" role="form">
    <div class="form-group">
      <label for="title">Movie Title</label>
      <input type="title" class="form-control" id="title" placeholder="Title" name="title">
    </div>
    <div class="form-group">
      <label for="image_url">Movie Art</label>
      <p>(Make sure not only link to open links i.e. Imgur and Wikipedia!!! Otherwise your picture may not show up and then no one will vote for your movie)</p>
      <input type="image_url" class="form-control" id="image_url" placeholder="Image URL" name="image_url">
    </div>
    <div class="form-group">
      <label for="description">Description</label>
      <p>Win us over with your flair for telling us why its the perfect movie to watch!</p>
      <input type="description" class="form-control" id="description" placeholder="OMG such a cool movie" name="description">
    </div>
    <button type="submit" class="btn btn-default">Submit</button>
  </form>
  <table>
  <% @choices.each do |choice| %>
    <tr>
      <td><img src="<%= choice.image_url %>" style="height:200px"></td>
      <td><h3><%= choice.title %></h3></td>
      <td><%= choice.description %></td>
      <td><h3><%= choice.votes == 1 ? "1 vote" : "#{choice.votes} votes"%></h3></td>
      <td><% if !@voted %><a href="/vote/<%= choice.id %>">Vote!</a><% else %>Sorry, you've already voted.<% end%></td>
    </tr>
  <% end %>
  </table>
</div>

<footer>
</footer>
